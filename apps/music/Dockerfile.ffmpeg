FROM ubuntu:24.04

# Install FFmpeg build dependencies + PulseAudio
RUN apt-get update && apt-get install -y \
    ffmpeg libpulse-dev pulseaudio-utils \
    && rm -rf /var/lib/apt/lists/*

ENTRYPOINT ["ffmpeg"]